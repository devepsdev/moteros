package dev.deveps.moteros.entities.enums;

/** Valores del ENUM `tipo` de la tabla `notificaciones`. */
public enum TipoNotificacion {
    like,
    comentario,
    solicitud_amistad,
    amistad_aceptada,
    inscripcion_quedada,
    nueva_quedada,
    quedada_cancelada,
    mensaje,
    valoracion_ruta
}
