package dev.deveps.moteros.entities.enums;

/** Valores del ENUM `estado` de la tabla `denuncias`. Resuelta: se tomo alguna medida. */
public enum EstadoDenuncia {
    pendiente, resuelta, descartada
}
