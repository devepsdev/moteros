package dev.deveps.moteros.entities.enums;

/** Valores del ENUM `tipo` de la tabla `denuncias`: que se denuncia. */
public enum TipoDenuncia {
    usuario, publicacion, comentario, mensaje, ruta, quedada
}
